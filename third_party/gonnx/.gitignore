# The full ONNX backend node-test corpus, regenerated by `make test_data`.
# Only the subset ops_test.go references is vendored, under test_data/, so the
# suite runs on a cold clone with no network. See README.md.
test_data_full/
temp_onnx/

.coverage.out

sample_models/.env
