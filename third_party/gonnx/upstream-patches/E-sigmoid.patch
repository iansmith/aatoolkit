diff --git a/ops/activation.go b/ops/activation.go
index 1fdee54..66a72dd 100644
--- a/ops/activation.go
+++ b/ops/activation.go
@@ -29,28 +29,42 @@ func Tanh(X tensor.Tensor) (tensor.Tensor, error) {
 }
 
 // Sigmoid performs the sigmoid operation on a tensor.
+//
+// It is computed as 0.5*(1 + tanh(x/2)), which is algebraically identical to the
+// textbook 1/(1+exp(-x)) but numerically stable: it exponentiates only through
+// tanh, whose implementation stays finite for every input. The direct form is not
+// safe here -- gorgonia's float32 tensor.Exp overflows to NaN/garbage once its
+// argument exceeds ~88 (exp(89)=NaN, exp(90)=-9.3e-39), so 1/(1+exp(-x)) returned
+// NaN for x < -88. Recurrent operators feed strongly-negative gate pre-activations
+// into Sigmoid once their state magnitude grows, so that NaN poisoned the LSTM cell
+// state on long real-audio runs of the Silero VAD model (SOP-89).
 func Sigmoid(X tensor.Tensor) (tensor.Tensor, error) {
-	negX, err := tensor.Neg(X)
+	typedHalf, err := GetValueAsTensorType(0.5, X.Dtype())
 	if err != nil {
 		return nil, err
 	}
 
-	expX, err := tensor.Exp(negX)
+	halfX, err := tensor.Mul(X, typedHalf)
 	if err != nil {
 		return nil, err
 	}
 
-	typedOne, err := GetValueAsTensorType(1.0, expX.Dtype())
+	tanhHalfX, err := tensor.Tanh(halfX)
 	if err != nil {
 		return nil, err
 	}
 
-	numeratorX, err := tensor.Add(typedOne, expX)
+	typedOne, err := GetValueAsTensorType(1.0, X.Dtype())
 	if err != nil {
 		return nil, err
 	}
 
-	return tensor.Div(typedOne, numeratorX)
+	onePlusTanh, err := tensor.Add(tanhHalfX, typedOne)
+	if err != nil {
+		return nil, err
+	}
+
+	return tensor.Mul(onePlusTanh, typedHalf)
 }
 
 // ReLU performs the ReLU operation on a tensor.
diff --git a/ops/activation_test.go b/ops/activation_test.go
index 5c27035..a30832d 100644
--- a/ops/activation_test.go
+++ b/ops/activation_test.go
@@ -20,5 +20,8 @@ func TestSigmoidActivation(t *testing.T) {
 	tOut, err := Sigmoid(tIn)
 
 	assert.Nil(t, err)
-	assert.Equal(t, []float32{0.7310586, 0.880797, 0.95257413, 0.98201376}, tOut.Data())
+	// Tolerance rather than exact equality: the numerically stable 0.5*(1+tanh(x/2))
+	// form differs from the textbook 1/(1+exp(-x)) form only in the last float32 ULP
+	// (e.g. sigmoid(2) = 0.8807971 vs 0.880797). Both round the true value correctly.
+	assert.InDeltaSlice(t, []float32{0.7310586, 0.880797, 0.95257413, 0.98201376}, tOut.Data(), 1e-6)
 }
