diff --git a/ops/gru/gru.go b/ops/gru/gru.go
index f5d97df..2099df6 100644
--- a/ops/gru/gru.go
+++ b/ops/gru/gru.go
@@ -32,6 +32,7 @@ type GRU struct {
 	direction         ops.SequenceProcessDirection
 	hiddenSize        int
 	linearBeforeReset bool
+	outputs           []string
 }
 
 // newGRU creates a new gru operator.
@@ -47,6 +48,7 @@ func newGRU(version int, typeConstraints [][]tensor.Dtype) ops.Operator {
 		activations:       []string{"sigmoid", "tanh"},
 		direction:         ops.Forward,
 		linearBeforeReset: false,
+		outputs:           []string{"Y", "Y_h"},
 	}
 }
 
@@ -84,6 +86,14 @@ func (g *GRU) Init(n *onnx.NodeProto) error {
 		}
 	}
 
+	// Capture the node's declared output names so Apply can return exactly that
+	// many tensors. Unlike LSTM, only overwrite when the node actually declares
+	// outputs -- callers that build a GRU node without an Output field (several
+	// unit tests do) then keep the {"Y","Y_h"} default and still get both.
+	if outputs := n.GetOutput(); len(outputs) > 0 {
+		g.outputs = outputs
+	}
+
 	return nil
 }
 
@@ -201,7 +211,14 @@ func (g *GRU) Apply(inputs []tensor.Tensor) ([]tensor.Tensor, error) {
 		return nil, err
 	}
 
-	return []tensor.Tensor{Y, Yh}, nil
+	// ONNX GRU outputs are positional (Y, Y_h) and the executor binds them
+	// positionally, so return them by position, truncated to the number the node
+	// declares (a node may request just Y or just Y_h -- both optional in ONNX).
+	// Cap at 2 so a malformed node declaring >2 outputs yields a length-mismatch
+	// error downstream rather than a slice panic.
+	nOutputs := min(len(g.outputs), 2)
+
+	return []tensor.Tensor{Y, Yh}[:nOutputs], nil
 }
 
 // extractXt extracts the value of x for timestep t.
diff --git a/ops/gru/gru_test.go b/ops/gru/gru_test.go
index 789f509..52aa179 100644
--- a/ops/gru/gru_test.go
+++ b/ops/gru/gru_test.go
@@ -1,11 +1,13 @@
 package gru
 
 import (
+	"fmt"
 	"testing"
 
 	"github.com/advancedclimatesystems/gonnx/onnx"
 	"github.com/advancedclimatesystems/gonnx/ops"
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 	"gorgonia.org/tensor"
 )
 
@@ -44,6 +46,20 @@ func TestGruInitUnkownAttr(t *testing.T) {
 	}
 }
 
+// layout=1 is the opset-14 batchwise layout, which GRU does not implement. Its
+// existing default: branch already rejects the unknown 'layout' attribute, so
+// this test locks that behavior rather than adding code. layout=0 is the ONNX
+// default (the attribute is omitted), which inits cleanly.
+func TestGRUInitRejectsLayout(t *testing.T) {
+	gru := GRU{}
+
+	err := gru.Init(&onnx.NodeProto{Attribute: []*onnx.AttributeProto{{Name: "layout", I: 1}}})
+	assert.Equal(t, ops.ErrInvalidAttribute("layout", &gru), err)
+
+	err = gru.Init(&onnx.NodeProto{})
+	assert.Nil(t, err)
+}
+
 func TestGru(t *testing.T) {
 	tests := []struct {
 		version  int64
@@ -129,11 +145,93 @@ func TestGru(t *testing.T) {
 		assert.Equal(t, test.err, err)
 
 		if err == nil {
-			assert.Equal(t, test.expected, res[1].Data())
+			// Tolerance not exact equality: the stable Sigmoid (0.5*(1+tanh(x/2)))
+			// shifts gate outputs by a last float32 ULP vs the old exp form (SOP-89).
+			assert.InDeltaSlice(t, test.expected, res[1].Data(), 1e-6)
 		}
 	}
 }
 
+// TestGruSigmoidSaturationRegression pins the SOP-76 divergence. Before SOP-89,
+// ops.Sigmoid computed 1/(1+exp(-x)) on gorgonia's float32 tensor.Exp, which
+// overflows past arg ~88 and returned 1.0 for a strongly-negative update-gate
+// pre-activation, so this GRU's hidden state came back {..., 0, 1} instead of the
+// correct {..., 0, 0}. onnxruntime 1.27.0 gives Y_h = [6.6936594e-3, 7.7486072e-7,
+// 0, 0] for these inputs (both linear_before_reset values). The 1e-3 tolerance is
+// deliberately far tighter than the 1.0 error the exp-overflow bug produced, so a
+// regression to the unstable sigmoid fails loudly, while still absorbing the benign
+// last-ULP differences between gonnx and onnxruntime.
+func TestGruSigmoidSaturationRegression(t *testing.T) {
+	// onnxruntime reference for gruInput0 with activations [sigmoid, tanh].
+	wantYh := []float32{6.6936594e-03, 7.7486072e-07, 0, 0}
+
+	for _, linearBeforeReset := range []int64{1, 0} {
+		// A subtest per variant so a failure (or FailNow from require) in one does
+		// not prevent the other from running -- SOP-76 requires both to be checked.
+		t.Run(fmt.Sprintf("linear_before_reset=%d", linearBeforeReset), func(t *testing.T) {
+			node := &onnx.NodeProto{
+				Attribute: []*onnx.AttributeProto{
+					{Name: "activation_alpha", Floats: []float32{}},
+					{Name: "activation_beta", Floats: []float32{}},
+					{Name: "activations", Strings: [][]byte{[]byte("sigmoid"), []byte("tanh")}},
+					{Name: "direction", S: []byte("forward")},
+					{Name: "hidden_size", I: 4},
+					{Name: "linear_before_reset", I: linearBeforeReset},
+				},
+			}
+
+			gru := gruVersions[7]()
+			require.NoError(t, gru.Init(node))
+
+			res, err := gru.Apply(gruInput0())
+			require.NoError(t, err)
+
+			assert.InDeltaSlicef(t, wantYh, res[1].Data(), 1e-3,
+				"GRU Y_h diverged from the onnxruntime reference (linear_before_reset=%d) -- "+
+					"a value near 1.0 in the last element means the exp-overflow Sigmoid regressed (SOP-76/SOP-89)",
+				linearBeforeReset)
+		})
+	}
+}
+
+// TestGruDeclaredOutputCount is the SOP-92 regression: GRU's Apply used to
+// hard-return two tensors, so a node declaring a different output count (ONNX
+// GRU outputs are all optional) failed at the executor's len(names) ==
+// len(outputTensors) check. Apply must now truncate its return to the declared
+// count -- mirroring the LSTM fix in SOP-87.
+func TestGruDeclaredOutputCount(t *testing.T) {
+	base := []*onnx.AttributeProto{
+		{Name: "activations", Strings: [][]byte{[]byte("sigmoid"), []byte("tanh")}},
+		{Name: "hidden_size", I: 4},
+	}
+
+	tests := []struct {
+		name    string
+		outputs []string
+		wantLen int
+	}{
+		{"single Y", []string{"Y"}, 1},
+		// A node that wants only Y_h encodes the absent Y as an empty string
+		// (ONNX optional-output convention), so Output has length 2 and both
+		// tensors come back -- the executor binds "" to Y (discarding it) and
+		// "Y_h" to Yh positionally.
+		{"Y_h only via empty Y slot", []string{"", "Y_h"}, 2},
+		{"both", []string{"Y", "Y_h"}, 2},
+		{"unspecified defaults to both", nil, 2},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			gru := gruVersions[7]()
+			require.NoError(t, gru.Init(&onnx.NodeProto{Output: tt.outputs, Attribute: base}))
+
+			res, err := gru.Apply(gruInput0())
+			require.NoError(t, err)
+			assert.Len(t, res, tt.wantLen)
+		})
+	}
+}
+
 func TestInputValidationGRU(t *testing.T) {
 	tests := []struct {
 		version  int64
diff --git a/ops/rnn/rnn.go b/ops/rnn/rnn.go
index 2649e24..dda6147 100644
--- a/ops/rnn/rnn.go
+++ b/ops/rnn/rnn.go
@@ -30,6 +30,7 @@ type RNN struct {
 	activations     []string
 	direction       ops.SequenceProcessDirection
 	hiddenSize      int
+	outputs         []string
 }
 
 // newRNN creates a new rnn operator.
@@ -44,6 +45,7 @@ func newRNN(version int, typeConstraints [][]tensor.Dtype) ops.Operator {
 		),
 		activations: []string{"tanh"},
 		direction:   ops.Forward,
+		outputs:     []string{"Y", "Y_h"},
 	}
 }
 
@@ -76,6 +78,14 @@ func (r *RNN) Init(n *onnx.NodeProto) error {
 		}
 	}
 
+	// Capture the node's declared output names so Apply can return exactly that
+	// many tensors. Unlike LSTM, only overwrite when the node actually declares
+	// outputs -- callers that build an RNN node without an Output field (several
+	// unit tests do) then keep the {"Y","Y_h"} default and still get both.
+	if outputs := n.GetOutput(); len(outputs) > 0 {
+		r.outputs = outputs
+	}
+
 	return nil
 }
 
@@ -170,7 +180,14 @@ func (r *RNN) Apply(inputs []tensor.Tensor) ([]tensor.Tensor, error) {
 		return nil, err
 	}
 
-	return []tensor.Tensor{Y, Yh}, nil
+	// ONNX RNN outputs are positional (Y, Y_h) and the executor binds them
+	// positionally, so return them by position, truncated to the number the node
+	// declares (a node may request just Y or just Y_h -- both optional in ONNX).
+	// Cap at 2 so a malformed node declaring >2 outputs yields a length-mismatch
+	// error downstream rather than a slice panic.
+	nOutputs := min(len(r.outputs), 2)
+
+	return []tensor.Tensor{Y, Yh}[:nOutputs], nil
 }
 
 // layerCalculation performs the actual RNN calculation. By ONNX definition
diff --git a/ops/rnn/rnn_test.go b/ops/rnn/rnn_test.go
index fb874ff..f25109c 100644
--- a/ops/rnn/rnn_test.go
+++ b/ops/rnn/rnn_test.go
@@ -7,6 +7,7 @@ import (
 	"github.com/advancedclimatesystems/gonnx/onnx"
 	"github.com/advancedclimatesystems/gonnx/ops"
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 	"gorgonia.org/tensor"
 )
 
@@ -34,6 +35,20 @@ func TestRNNInitUnknownAttr(t *testing.T) {
 	assert.Equal(t, err, ops.ErrInvalidAttribute("unknown", &rnn))
 }
 
+// layout=1 is the opset-14 batchwise layout, which RNN does not implement. Its
+// existing default: branch already rejects the unknown 'layout' attribute, so
+// this test locks that behavior rather than adding code. layout=0 is the ONNX
+// default (the attribute is omitted), which inits cleanly.
+func TestRNNInitRejectsLayout(t *testing.T) {
+	rnn := &RNN{}
+
+	err := rnn.Init(&onnx.NodeProto{Attribute: []*onnx.AttributeProto{{Name: "layout", I: 1}}})
+	assert.Equal(t, ops.ErrInvalidAttribute("layout", rnn), err)
+
+	err = rnn.Init(&onnx.NodeProto{})
+	assert.Nil(t, err)
+}
+
 func TestRNN(t *testing.T) {
 	tests := []struct {
 		version  int64
@@ -148,11 +163,52 @@ func TestRNN(t *testing.T) {
 		assert.Equal(t, test.err, err)
 
 		if err == nil {
-			assert.Equal(t, test.expected, res[1].Data())
+			// Tolerance not exact equality: the stable Sigmoid (0.5*(1+tanh(x/2)))
+			// shifts sigmoid-activated outputs by a last float32 ULP vs the old exp
+			// form (SOP-89).
+			assert.InDeltaSlice(t, test.expected, res[1].Data(), 1e-6)
 		}
 	}
 }
 
+// TestRNNDeclaredOutputCount is the SOP-92 regression: RNN's Apply used to
+// hard-return two tensors, so a node declaring a different output count (ONNX
+// RNN outputs are all optional) failed at the executor's len(names) ==
+// len(outputTensors) check. Apply must now truncate its return to the declared
+// count -- mirroring the LSTM fix in SOP-87.
+func TestRNNDeclaredOutputCount(t *testing.T) {
+	base := []*onnx.AttributeProto{
+		{Name: "activations", Strings: [][]byte{[]byte("tanh")}},
+		{Name: "hidden_size", I: 4},
+	}
+
+	tests := []struct {
+		name    string
+		outputs []string
+		wantLen int
+	}{
+		{"single Y", []string{"Y"}, 1},
+		// A node that wants only Y_h encodes the absent Y as an empty string
+		// (ONNX optional-output convention), so Output has length 2 and both
+		// tensors come back -- the executor binds "" to Y (discarding it) and
+		// "Y_h" to Yh positionally.
+		{"Y_h only via empty Y slot", []string{"", "Y_h"}, 2},
+		{"both", []string{"Y", "Y_h"}, 2},
+		{"unspecified defaults to both", nil, 2},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			rnn := rnnVersions[7]()
+			require.NoError(t, rnn.Init(&onnx.NodeProto{Output: tt.outputs, Attribute: base}))
+
+			res, err := rnn.Apply(rnnInput0())
+			require.NoError(t, err)
+			assert.Len(t, res, tt.wantLen)
+		})
+	}
+}
+
 func TestInputValidationRNN(t *testing.T) {
 	tests := []struct {
 		version  int64
